# 伺服器事業部入口網站 專案架構規格

版本：v1.0（討論定稿，待確認後移至 Claude Code 實作）

## 一、專案背景與目標

目前部門已有「私車公用報支系統」（已完成）與「出差申報系統」（撰寫中），兩者各自獨立運作、各自維護一份人員清單。這次要建立一個統一入口網站，讓同仁透過單一入口登入後，選擇要使用的系統；未來新系統上線時，直接在入口網站中新增選項即可，不需要重新架設。

## 二、技術架構

**框架**：Python + Streamlit。

**執行架構**：採用「單一 Streamlit 多頁面應用」，而非多個各自獨立的 Streamlit process。原因是同仁登入後，兩個報支系統需要自動帶入登入者的身分資料（姓名、部門、員工編號、職務），單一應用內的 `session_state` 可以在頁面間自然共享，不需要額外做跨程式的身分傳遞機制。

**Repo 拆分**（開發面獨立、部署面組裝）：

- `shared-core`：共用核心模組，包含資料庫存取、登入驗證邏輯、原有的版本標籤/署名浮水印元件。三個系統都依賴這個 repo。
- `portal-app`：入口網站本體，包含登入頁、選單頁、管理員維護區。
- `car-expense-app`：私車公用報支頁面模組（沿用現有已測試過的核心邏輯，只改造最上方的身分輸入區塊）。
- `trip-expense-app`：出差申報頁面模組（從一開始就依新規則撰寫）。

部署時，`portal-app` 透過 Git Submodule 把 `car-expense-app` 與 `trip-expense-app` 掛進 `pages/` 目錄下，組成一份完整可執行的程式；日常開發則各自在自己的 repo 內進行，互不干擾。

**部署環境**：雲端主機，可能對外可存取，需啟用 HTTPS。

## 三、登入與身分驗證設計

- 入口網站登入：帳號＝員工編號，密碼＝全體同仁共用的一組統一密碼（雜湊儲存，非明文）。
- 登入成功後，系統記住目前登入者對應共用員工資料表的哪一筆紀錄。
- 同仁點選私車公用報支或出差申報後，**不需要再次輸入帳密**，系統直接依登入身分自動帶入姓名、部門/單位、員工編號、職務等欄位，同仁確認無誤後送出即可（取代原本兩系統各自的手動下拉選單）。
- 管理員維護區：選單上的一個入口，同仁看到但點進去需要**再輸入一組獨立的管理員密碼**（第二層驗證），與同仁的統一密碼分開存放。

## 四、共用員工資料表（employees）

| 欄位 | 說明 |
|---|---|
| 單位/部門 | 兩系統欄位名稱統一 |
| 員工編號 | 同時作為登入帳號 |
| 姓名 | |
| 職務 | 出差申報表單會使用；私車公用報支目前不使用 |
| 在職狀態 | 布林值，預設為在職 |

- 私車公用報支使用欄位：單位、姓名。
- 出差申報使用欄位：員工編號、姓名、單位、職務。
- 離職處理：**不做實體刪除**。管理員在維護區將該員工列的核取欄位取消，系統只是把「在職狀態」設為停用，該員工的登入帳號即失效，但姓名等歷史資料保留（避免過去報支紀錄對不起來）。畫面上該列核取欄位反灰。
- 假設管理員可以再次將反灰的員工恢復為在職狀態（例如點錯或員工回任），若這點你希望是不可逆的一次性動作，麻煩告知，我會拿掉恢復功能。

**資料庫**：SQLite（檔案型資料庫，符合目前部門規模、雲端主機部署單純；未來規模變大可平順遷移至 PostgreSQL）。

## 五、系統設定（system_settings）

- 全體同仁共用的統一登入密碼（雜湊）
- 管理員密碼（雜湊）

## 六、管理員維護區功能

1. **部門員工資料維護**：以表格呈現現有員工，前方核取欄位可停用（離職）帳號；表格下方有「＋」列可新增員工。
2. **上傳操作SOP**：管理員可分別針對私車公用報支、出差申報上傳各自的 PDF 操作手冊。同仁登入對應系統的頁面時，會有「查看操作SOP」按鈕可開啟/下載目前版本。上傳新版本時直接覆蓋舊版本，不保留歷史版本（如需保留歷史版本供管理員回溯，可以加做，工作量會略增，需要的話請告知）。
3. **更新登入密碼**：修改全體同仁共用的統一登入密碼。

## 七、開發與測試流程

- 各子系統（`car-expense-app`、`trip-expense-app`）自己的 repo 內建立「假身分測試進入點」，開發時可直接用假的員工資料 `streamlit run` 該子系統，獨立驗證畫面與邏輯，不需要每次都跑完整的入口網站登入流程。
- 正式要更新到入口網站正式版本（更新 submodule 版本）之前，需完整跑一次「登入入口網站 → 點選該系統」的整合驗證，確認身分資料傳遞與選單串接正常。

## 八之一、視覺設計

- **底圖**：登入頁與登入後的選單頁（管理維護區/私車公用報支/出差申報三個選項），統一採用同一張伺服器機房意象底圖（素材已存檔為 `portal_background.jpg`）。進入私車公用報支、出差申報這兩個實際工作表單頁面後，維持原本的白底工作畫面，不套用底圖。
- **登入卡片**：底圖上疊一張白色圓角卡片，內容為「Login」標題、帳號欄、密碼欄（含顯示/隱藏密碼圖示）、藍色「Log In」按鈕。**不含**「Forgot Password?」連結——因為密碼是全體同仁共用一組、僅管理員可修改，沒有自助重設的需求，畫面上只保留帳號、密碼、登入按鈕。

## 八、確認事項（已定案）

- 反灰後的員工帳號，管理員可以再次恢復其登入資格。
- SOP 不保留歷史版本，上傳新版本直接覆蓋舊版本。
- 私車公用報支現有程式碼**全部沿用**，唯一要改的地方是：原本由使用者手動選擇姓名的輸入方式，改為登入入口網站時就已綁定姓名（連同部門、員工編號等身分資料），表單直接帶入、同仁確認即可，不需要再手動選擇。
